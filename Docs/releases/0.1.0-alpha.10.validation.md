# 0.1.0-alpha.10 validation

Release build completed with warnings treated as errors. SchedulerSimulation passed, including 8,192 exhaustive matching graphs, 1,000 random brute-force comparisons and the existing clinical, ownership and lifecycle cases. Package validation, bilingual payload validation and Steam session helper tests passed. Online English and Chinese Workshop descriptions matched the local descriptions before publishing.

Recent game evidence is recorded in the 2026-09-13 matching, scoring, standing/cache and extraction-recovery validation directories. The final source-index/standing regression passed 93 checks. Large-scenario measurements include probe overhead and varying clinical trajectories; no claim of equal clinical outcomes or universal TPS gains is made.

The full `dotnet format --verify-no-changes --severity warn` check reports whitespace differences, including existing unchanged files such as FieldTreatmentBoundary.cs and CasevacScheduling.cs. The release build and package checks pass; repository-wide formatting remains outstanding.
